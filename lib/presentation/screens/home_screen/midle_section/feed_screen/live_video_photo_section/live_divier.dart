import 'package:flutter/material.dart';

class LiveDivier extends StatelessWidget {
  const LiveDivier({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 15,
      width: 1,
      color: Colors.grey[300],
    );
  }
}
