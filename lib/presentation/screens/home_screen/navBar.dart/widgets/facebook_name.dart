import 'package:flutter/material.dart';

class FacebookName extends StatelessWidget {
  const FacebookName({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text(
      ' facebook',
      style: TextStyle(
        color: Colors.blue,
        fontSize: 30,
        fontWeight: FontWeight.bold,
      ),
    );
  }
}
